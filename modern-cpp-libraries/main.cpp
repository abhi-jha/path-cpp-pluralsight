#include "Precompiled.h"

static_assert(sizeof(double) == 8, "can't serialize floats");

#include <cassert>


//FIND A BETTER TUTORIAL WHICH IS PLATFORM INDEPENDENT
auto main() -> int{

    //assert(4 == 5);

    //static_assert(sizeof(double) == 4, "can't serialize floats");

}